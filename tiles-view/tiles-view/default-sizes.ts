import { TilesConfig } from '../config/tiles-config';

export const DEFAULT_TILES_CONFIG_S: TilesConfig = {
  tileHeight: 160,
  tileWidth: 240,
  leftRightMargin: 8,
  topBottomMargin: 8
};

export const DEFAULT_TILES_CONFIG_M: TilesConfig = {
  tileHeight: 180,
  tileWidth: 280,
  leftRightMargin: 8,
  topBottomMargin: 8
};

export const DEFAULT_TILES_CONFIG_L: TilesConfig = {
  tileHeight: 200,
  tileWidth: 320,
  leftRightMargin: 8,
  topBottomMargin: 8
};
