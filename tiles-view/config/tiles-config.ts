export interface TilesConfig {
  // tile height in px
  tileHeight: number;

  // tile width in px
  tileWidth: number;

  // tile left and right margin in px
  leftRightMargin: number;

  // tile top and  bottom margin in px
  topBottomMargin: number;
}
